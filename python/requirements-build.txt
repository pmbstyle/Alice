# Build-time requirements (minimal dependencies for PyInstaller)
# AI libraries will be installed at runtime

# FastAPI and server dependencies
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.0.0
python-multipart>=0.0.6
aiofiles>=23.0.0

# Core utilities (lightweight)
numpy>=1.24.3